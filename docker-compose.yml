version: '2'
services:
  certbot:
    build: .
    environment:
    - EMAIL=thomas15v@gmail.com
    - DOMAINS=somkotest.tk
    ports:
    - 8080:8080
  nginx:
    image: nginx:alpine
    ports:
      - 443:443
      - 80:80
    volumes:
     - ./default.conf:/etc/nginx/conf.d/site.conf
    links:
     - certbot
    volumes_from:
    - certbot